[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "colight-serde"
version = "0.1.0"
description = "Binary format for efficient array transfer."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.22,<2.0.0",
]

[[project.authors]]
name = "Matthew Huebert"
email = "mhuebert@gmail.com"

[project.license]
text = "Apache-2.0"

[tool.hatch.build]
ignore-vcs = true

[tool.hatch.build.targets.wheel]
packages = ["src/colight_serde"]
