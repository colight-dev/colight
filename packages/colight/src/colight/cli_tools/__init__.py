"""Agent-facing CLI helpers: block graph, headless runs, artifact inspection.

These modules back the ``colight blocks``, ``colight run``,
``colight inspect``, ``colight diff``, ``colight screenshot`` and
``colight verify`` subcommands. They are importable so tests exercise the
same code paths as the CLI.
"""
