"""Agent-facing CLI helpers: block graph, headless runs, artifact inspection.

These modules back the ``colight blocks``, ``colight run`` and
``colight inspect`` subcommands. They are importable so tests exercise the
same code paths as the CLI.
"""
